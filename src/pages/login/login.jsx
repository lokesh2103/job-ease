import { Button } from "@mui/material";

function Login() {
  return (
    <div>
      <div>
        <Button
          variant="contained"
          color="primary"
          style={{ marginTop: "20px" }}
        >
          Sign Up
        </Button>
      </div>
    </div>
  );
}

export default Login;
